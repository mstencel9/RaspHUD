<RPM_Screen>:
    BoxLayout:
        orientation: 'vertical'
        Label:
            id: rpm_label
            text: "RPM: " + str(app.rpm)
            font_size: 48
            halign: 'center'
            valign: 'middle'
